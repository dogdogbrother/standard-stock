# 📱 移动端调试工具 - vConsole 使用指南

## 🎯 什么是 vConsole？

vConsole 是腾讯开源的移动端网页前端调试面板，类似于电脑上的 Chrome DevTools，但专为移动端设计。

---

## ✅ 已安装并配置

### 自动启用条件
- ✅ **开发环境**（`npm run dev`）
- ✅ **移动设备**（手机/平板浏览器）

### 不会启用的情况
- ❌ 生产环境（打包后）
- ❌ 电脑浏览器（已有 DevTools）

---

## 🚀 如何使用

### 1. 启动开发服务器

```bash
npm run dev
```

### 2. 在真机上访问

**方法 A：通过 IP 地址**
```
http://你的电脑IP:5173
```

例如：`http://192.168.1.100:5173`

**方法 B：通过二维码**
- 使用 `npm run dev` 显示的二维码
- 手机扫码访问

### 3. 查看 vConsole 面板

在手机浏览器中打开应用后，你会看到屏幕右下角有一个**绿色圆形按钮**（vConsole 入口）。

**点击按钮**，展开调试面板！

---

## 📊 功能介绍

### 1️⃣ Console 标签
- 查看所有 `console.log()` 输出
- 查看错误信息
- 查看警告信息

**使用场景：**
- 查看 "初始化数据失败" 的具体错误
- 查看 API 请求的参数
- 调试业务逻辑

### 2️⃣ Network 标签
- 查看所有网络请求
- 请求 URL、状态码、耗时
- 请求和响应的详细信息

**使用场景：**
- 查看 `stock-detail` API 是否超时
- 查看 Edge Function 返回的错误信息
- 分析哪个请求导致页面慢

### 3️⃣ Storage 标签
- 查看 localStorage
- 查看 sessionStorage
- 查看 Cookie

**使用场景：**
- 查看 Pinia 缓存的数据
- 调试登录状态
- 清除缓存测试

### 4️⃣ System 标签
- 查看设备信息
- 查看 User Agent
- 查看屏幕尺寸

**使用场景：**
- 确认是否识别为移动设备
- 查看浏览器版本

---

## 🔍 调试 Profile 页面加载慢的问题

### 步骤 1：打开 Network 标签

在 vConsole 中切换到 **Network** 标签

### 步骤 2：刷新页面

在 Profile 页面下拉刷新或重新进入

### 步骤 3：查看请求列表

**重点关注：**

1. **money 查询**
   ```
   POST https://qixncbgvrkfjxopqqpiz.supabase.co/rest/v1/money
   ```
   - 状态：200？
   - 耗时：多少？

2. **position 查询**
   ```
   POST https://qixncbgvrkfjxopqqpiz.supabase.co/rest/v1/position
   ```
   - 状态：200？
   - 耗时：多少？

3. **股票实时数据**
   ```
   GET https://qixncbgvrkfjxopqqpiz.supabase.co/functions/v1/stock-detail
   ```
   - 状态：200？还是超时？
   - 耗时：**如果超过 5 秒，这就是慢的原因！**

### 步骤 4：点击请求查看详情

点击 `stock-detail` 请求，查看：
- **Request Headers**：发送的参数
- **Response**：返回的数据或错误信息
- **Duration**：耗时

---

## 💡 常见问题排查

### Q1: 看不到 vConsole 按钮？

**原因：**
- 不是移动设备
- 不是开发环境

**解决：**
- 确认在真机上访问
- 确认使用 `npm run dev` 启动
- 尝试刷新页面

### Q2: Network 标签没有请求？

**原因：**
- 页面还没有发起请求
- 请求在 vConsole 加载前就完成了

**解决：**
- 打开 vConsole 后刷新页面
- 触发操作（如下拉刷新）

### Q3: Console 输出太多看不清？

**解决：**
- 点击右上角 **Clear** 清空日志
- 使用 Filter 过滤关键词
- 只关注 Error 类型

---

## 🎨 自定义配置

如果需要调整 vConsole 的配置，编辑 `src/main.ts`：

```typescript
new VConsole({
  theme: 'light',  // 改为浅色主题
  defaultPlugins: ['system', 'network', 'element', 'storage'],
  maxLogNumber: 5000,  // 增加最大日志数
  onReady: () => {
    console.log('vConsole 已就绪')
  }
})
```

---

## 🔧 调试技巧

### 技巧 1：添加关键日志

在怀疑慢的地方添加日志：

```typescript
// src/stores/position.ts
console.log('⏰ 开始请求股票实时数据', new Date().toISOString())

const response = await fetch(apiUrl)

console.log('✅ 股票数据请求完成', new Date().toISOString())
```

在 vConsole 的 Console 标签中查看时间差。

### 技巧 2：使用 console.time

```typescript
console.time('fetchPositions')
await positionStore.fetchPositions(true)
console.timeEnd('fetchPositions')
// 输出：fetchPositions: 3425.123ms
```

### 技巧 3：捕获并打印错误

```typescript
try {
  await initializeData()
} catch (err) {
  console.error('❌ 初始化失败:', err)
  console.error('详细信息:', JSON.stringify(err, null, 2))
}
```

---

## 📸 截图说明

### vConsole 面板示例

**Console 标签：**
```
[Log] ⏰ 开始请求股票实时数据
[Log] ✅ 股票数据请求完成
[Error] ❌ 初始化失败: Error: 查询资金失败
```

**Network 标签：**
```
GET /functions/v1/stock-detail  200  3.2s
POST /rest/v1/position          200  0.5s
POST /rest/v1/money             200  0.3s
```

---

## 🎯 下一步

### 现在可以做的事：

1. **启动服务器**
   ```bash
   npm run dev
   ```

2. **在真机上访问**
   - 使用 IP 地址或二维码

3. **打开 Profile 页面**
   - 点击右下角绿色按钮
   - 打开 Network 标签
   - 刷新页面

4. **查看哪个请求慢**
   - 如果 `stock-detail` 超过 5 秒 → API 慢
   - 如果 `position` 超过 2 秒 → 数据库查询慢
   - 如果 `dividend` 超过 2 秒 → 股息率查询慢

5. **根据结果优化**
   - 添加超时控制
   - 优化查询语句
   - 使用缓存策略

---

## 📚 相关资源

- 官方文档：https://github.com/Tencent/vConsole
- 配置选项：https://github.com/Tencent/vConsole/blob/dev/doc/tutorial_CN.md

---

## ✅ 总结

vConsole 让你可以在真机上：
- ✅ 查看控制台输出
- ✅ 监控网络请求
- ✅ 调试本地存储
- ✅ 排查性能问题

**现在在真机上打开应用，看看右下角的绿色按钮吧！** 📱🔍

